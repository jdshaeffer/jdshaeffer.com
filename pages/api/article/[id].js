import db from '../../../lib/db';
import a from 'sql-template-strings';

export default async (req, res) => {
  const {
    query: { id },
  } = req;

  // get one article
  if (req.method === 'GET') {
    
  }
};